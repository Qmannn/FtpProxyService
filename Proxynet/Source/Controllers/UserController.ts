export namespace App.Controllers {
    export class UserController {
        public getUsers(count: number): Array<string> {
            return new Array<string>(count);
        }
    }
}

export class UserController {
        public getUsers(count: number): Array<string> {
            return new Array<string>(count);
        }
    }