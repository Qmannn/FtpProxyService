<div ng-controller="UsersController as ctrl">
    <h2>Пользователи</h2>
    <table class="table">
        <tr>
            <th>Логин</th> 
            <th>Имя</th> 
            <th>Группы</th> 
            <th>
                Дейтсвия
            </th> 
        </tr>
        <tr ng-repeat="user in ctrl.users">
            <td>{{user.login}}</td>
            <td>{{user.name}}</td>
            <td>
                <div class="col-md-5">
                    <a href="#groups-spoiler-{{user.id}}" class="btn-link" data-toggle="collapse">Показать</a>
                    <div class="collapse" id="groups-spoiler-{{user.id}}">
                        <div class="well">
                            <ul>
                               <li ng-repeat="group in user.groups">{{group.name}}</li> 
                            </ul>
                        </div>
                    </div>
                </div>
            </td>
            <td>
                <a href="ftpproxy/user/{{user.id}}" class="btn btn-default">Изменить</a>
            </td>
        </tr>
    </table>
</div>

